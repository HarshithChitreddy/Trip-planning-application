package com.tco.misc;

import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.DisplayName;

import static org.junit.jupiter.api.Assertions.assertThrows;
import static org.junit.jupiter.api.Assertions.assertTrue;

public class TestPerson {

        /*
    @Test
    @DisplayName("base: Values present on good ctor")
    public void testGoodPerson() {
        Person goodPerson = new Person("Cam the Ram", "NetID", "Fort Collins, CO", "Biography");
        assertTrue(goodPerson.get("name").equals("Cam the Ram"));
        assertTrue(goodPerson.get("hometown").equals("Fort Collins, CO"));
        assertTrue(goodPerson.get("biography").equals("Biography"));
        assertTrue(goodPerson.get("netid").equals("NetID"));
    }


    @Test
    @DisplayName("base: Null values throw NPE when passed to ctor")
    public void testBadPersonNullCases() {
        assertThrows(NullPointerException.class, () -> new Person(null, "Fort Collins, CO", "Biography", "NetID"));
        assertThrows(NullPointerException.class, () -> new Person("Cam the Ram", null, "Biography", "NetID"));
        assertThrows(NullPointerException.class, () -> new Person("Cam the Ram", "Fort Collins, CO", null, "NetID"));
        assertThrows(NullPointerException.class, () -> new Person("Cam the Ram", "Fort Collins, CO", "Biography", null));
    }

     */


}